# /mpga:status

Show the MPGA project health dashboard.

## Steps

1. Run `node ./.mpga-runtime/cli/dist/index.js status` and display the output
2. If MPGA not initialized → suggest `/mpga:init`
3. Highlight any issues (stale evidence, blocked tasks)

## Usage
```
/mpga:status
```
